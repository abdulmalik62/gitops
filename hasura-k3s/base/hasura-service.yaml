apiVersion: v1
kind: Service
metadata:
  name: hasura-service
  namespace: default  # change if you're using another namespace
spec:
  type: NodePort
  selector:
    app: hasura
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 32080  # Custom nodePort (must be between 30000–32767)